<UserControl x:Class="MtmEquipmentApp.Views.Pages.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MtmEquipmentApp.Views.Pages"
             xmlns:viewModels="clr-namespace:MtmEquipmentApp.ViewModel.Users"
             Height="450" Width="800">

    <UserControl.DataContext>
        <viewModels:UserPageViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Button Content="Добавить пользователя"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Command="{Binding AddUser}" />

        <DataGrid ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  Margin="0,60,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя"
                                    Binding="{Binding FullName}" />
                <DataGridTextColumn Header="Логин"
                                    Binding="{Binding Login}" />
                <DataGridTextColumn Header="Роль"
                                    Binding="{Binding Role}" />
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Редактировать"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Command="{Binding EditUser}"
                Margin="0,0,100,20" />

        <Button Content="Удалить"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Command="{Binding DeleteUser}"
                Margin="0,0,20,20" />
    </Grid>
</UserControl>
