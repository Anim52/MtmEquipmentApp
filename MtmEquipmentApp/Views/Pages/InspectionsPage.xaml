<UserControl x:Class="MtmEquipmentApp.Views.Pages.InspectionsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MtmEquipmentApp.Views.Pages"
             >

    <UserControl.Resources>
        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="18"
                          ShadowDepth="0"
                          Opacity="0.08"
                          Color="#000000"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.80"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#E2E8F0"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="ModernDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Height" Value="44"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="MinHeight" Value="46"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border x:Name="RowBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Margin="0,2,0,2"
                                SnapsToDevicePixels="True">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </SelectiveScrollingGrid.RowDefinitions>

                                <DataGridCellsPresenter Grid.Column="1"/>
                            </SelectiveScrollingGrid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RowBorder" Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RowBorder" Property="Background" Value="#1D4ED8"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Margin="0,0,0,12"
                Padding="26"
                Background="White"
                CornerRadius="22"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <Border Background="#EEF4FF"
                            CornerRadius="10"
                            Padding="10,5"
                            HorizontalAlignment="Left"
                            Margin="0,0,0,12">
                        <TextBlock Text="Журнал контроля"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="#2563EB"/>
                    </Border>

                    <TextBlock Text="Инспекции"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="#0F172A"/>

                    <TextBlock Text="Журнал проверок оборудования"
                               Margin="0,6,0,0"
                               FontSize="14"
                               Foreground="#64748B"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Button Content="Добавить"
                            Margin="0,0,10,0"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding AddCommand}"/>

                    <Button Content="Обновить"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Фильтр -->
        <Border Grid.Row="1"
                Margin="20,0,20,12"
                Padding="18"
                Background="White"
                CornerRadius="22"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Background="White"
                        BorderBrush="#111827"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="10,2"
                        Margin="0,0,12,0">
                    <DatePicker SelectedDate="{Binding DateFrom}"
                                Style="{StaticResource ModernDatePickerStyle}"/>
                </Border>

                <Border Grid.Column="1"
                        Background="White"
                        BorderBrush="#111827"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="10,2"
                        Margin="0,0,12,0">
                    <DatePicker SelectedDate="{Binding DateTo}"
                                Style="{StaticResource ModernDatePickerStyle}"/>
                </Border>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="История оборудования"
                            Margin="0,0,10,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding OpenEquipmentHistoryCommand}"/>

                    <Button Content="Редактировать"
                            Margin="0,0,10,0"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding EditCommand}"/>

                    <Button Content="Удалить"
                            Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding DeleteCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Таблица -->
        <Border Grid.Row="2"
                Margin="20,0,20,20"
                Padding="14"
                Background="White"
                CornerRadius="22"
                Effect="{StaticResource CardShadow}">

            <DataGrid ItemsSource="{Binding Inspections}"
                      SelectedItem="{Binding SelectedInspection}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      BorderThickness="0"
                      Background="Transparent"
                      RowBackground="White"
                      AlternatingRowBackground="#F8FAFC"
                      ColumnHeaderHeight="44"
                      RowHeight="46"
                      FontSize="14"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      MouseDoubleClick="InspectionsGrid_MouseDoubleClick">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Дата"
                                        Binding="{Binding Date}"
                                        Width="140"/>

                    <DataGridTextColumn Header="Оборудование"
                                        Binding="{Binding Equipment.Name}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Инв. номер"
                                        Binding="{Binding Equipment.InventoryNumber}"
                                        Width="160"/>

                    <DataGridTextColumn Header="Инспектор"
                                        Binding="{Binding Inspector.FullName}"
                                        Width="200"/>

                    <DataGridCheckBoxColumn Header="Дефект"
                                            Binding="{Binding IsDefective}"
                                            Width="100"/>

                    <DataGridTextColumn Header="Комментарий"
                                        Binding="{Binding Remarks}"
                                        Width="300"/>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>
</UserControl>