<Window x:Class="MtmEquipmentApp.Views.Windows.EquipmentHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="История оборудования"
        Height="650"
        Width="860"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC"
        Icon="/Recources/Image/Image.ico">
    <Window.Resources>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="24" ShadowDepth="0" Opacity="0.12" Color="#000000"/>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/><Setter Property="MinWidth" Value="110"/>
            <Setter Property="Foreground" Value="White"/><Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="Transparent"/><Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Padding" Value="18,0"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/><Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Padding" Value="12,0"/>
            <Setter Property="Height" Value="38"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="#0F172A"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/><Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Border Padding="24" Background="White" CornerRadius="22" Effect="{StaticResource WindowShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <Border Background="#EEF4FF" CornerRadius="10" Padding="10,5" HorizontalAlignment="Left" Margin="0,0,0,12">
                        <TextBlock Text="История" FontSize="12" FontWeight="SemiBold" Foreground="#2563EB"/>
                    </Border>
                    <TextBlock Text="{Binding EquipmentName}" FontSize="28" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="{Binding InventoryNumber, StringFormat=Инвентарный номер: {0}}" Margin="0,6,0,0" FontSize="14" Foreground="#64748B"/>
                </StackPanel>

                <UniformGrid Grid.Row="1" Columns="4" Margin="0,22,0,16">
                    <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="14" Padding="14" Margin="0,0,8,0">
                        <StackPanel><TextBlock Text="Подразделение" Foreground="#64748B"/><TextBlock Text="{Binding DepartmentName}" FontWeight="Bold" FontSize="16"/></StackPanel>
                    </Border>
                    <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="14" Padding="14" Margin="8,0">
                        <StackPanel><TextBlock Text="Дата покупки" Foreground="#64748B"/><TextBlock Text="{Binding PurchaseDate}" FontWeight="Bold" FontSize="16"/></StackPanel>
                    </Border>
                    <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="14" Padding="14" Margin="8,0">
                        <StackPanel><TextBlock Text="Проверок" Foreground="#64748B"/><TextBlock Text="{Binding InspectionsCount}" FontWeight="Bold" FontSize="22"/></StackPanel>
                    </Border>
                    <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="14" Padding="14" Margin="8,0,0,0">
                        <StackPanel><TextBlock Text="Ремонтов/поломок" Foreground="#64748B"/><TextBlock Text="{Binding RepairsCount}" FontWeight="Bold" FontSize="22"/></StackPanel>
                    </Border>
                </UniformGrid>

                <Border Grid.Row="2" Background="#FFF7ED" BorderBrush="#FDBA74" BorderThickness="1" CornerRadius="14" Padding="14" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Причины поломок" FontWeight="Bold" Foreground="#7C2D12" Margin="0,0,0,6"/>
                        <TextBlock Text="{Binding DefectReasons}" TextWrapping="Wrap" Foreground="#0F172A"/>
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="3" ItemsSource="{Binding Inspections}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="1" BorderBrush="#CBD5E1" RowBackground="White" AlternatingRowBackground="#F8FAFC" SelectionMode="Single" SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Инспектор" Binding="{Binding Inspector.FullName}" Width="190"/>
                        <DataGridCheckBoxColumn Header="Дефект" Binding="{Binding IsDefective}" Width="90"/>
                        <DataGridTextColumn Header="Комментарий" Binding="{Binding Remarks}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="4" Content="Закрыть" HorizontalAlignment="Right" Margin="0,18,0,0" Style="{StaticResource PrimaryButtonStyle}" Click="Close_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
